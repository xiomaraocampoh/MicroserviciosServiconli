spring:
  datasource:
    url: jdbc:mysql://localhost:3306/data_central_de_citas?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  application:
    name: task-service

# Segunda base de datos: serviconli2025
serviconli:
  datasource:
    url: jdbc:mysql://localhost:3306/serviconli2025?useSSL=false&serverTimezone=UTC
    username: root
    password:

server:
  port: 8082

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
    com:
      serviconli: DEBUG
